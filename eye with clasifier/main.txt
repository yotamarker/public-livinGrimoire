Imports System.Math
Imports WindowsApp1.rinegan

Public Class Form1
    Dim eye1 As New Aeye()

    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click

        Dim bmp As New Bitmap(imageLocaiton.Text)
        PictureBox1.Image = bmp.Clone()

        Dim ImageList As New List(Of Rectangle)
        eye1.setClusterPercent(0.08)
        'eye1.PopulateEyeData2(bmp, ImageList)
        eye1.shiberActivator(bmp)
        Dim ecTest As New EyeClasifire()
        eye1.shiberVision(bmp, ImageList, ecTest)
        'Aeye.DirectionGetter(bmp)




        PictureBox2.Image = bmp.Clone()

        TextBox3.Text = "image height : " & bmp.Height.ToString & " image width : " & bmp.Width.ToString & " " & Aeye.DirectionGetter(bmp)


    End Sub

    Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        Dim bmp As New Bitmap("c:\testimage\x1.bmp")
        PictureBox1.Image = bmp.Clone()
    End Sub

    Private Sub Button2_Click(sender As Object, e As EventArgs) Handles Button2.Click
        'Dim bmp As New Bitmap(imageLocaiton.Text)
        'PictureBox1.Image = bmp.Clone()
        'Dim x As Integer = 3
        'Dim y As Integer = 3

        'Dim miscInfo As String = [String].Format("from {0} to {1} on the X axis", (x - 1) * bmp.Width \ 3, x * bmp.Width \ 3)
        'miscInfo &= vbCrLf & [String].Format("from {0} to {1} on the Y axis", (y - 1) * bmp.Height \ 3, y * bmp.Height \ 3)
        'TextBox3.Text = miscInfo & vbCrLf & bmp.Width & "   " & bmp.Height

        ' enable night rider deco
        'Timer1.Enabled = Not Timer1.Enabled
    End Sub

    Private Sub Timer1_Tick(sender As Object, e As EventArgs) Handles Timer1.Tick
        ' night rider decoration :
        'Static Dim BoolArray(9) As Boolean
        'Static Dim counter As Integer = 0
        'Static Dim toAdd As Integer = 1
        'BoolArray(counter) = False
        'counter += toAdd
        'BoolArray(counter) = True
        'If counter > 8 Then
        '    toAdd = -1
        'ElseIf counter < 1 Then
        '    toAdd = 1
        'End If
        'Dim NightRider As String = ""
        'For index = 0 To BoolArray.Length - 1
        '    If BoolArray(index) Then
        '        NightRider &= "1"
        '    Else
        '        NightRider &= "0"

        '    End If
        'Next
        'TextBox3.Text = NightRider
    End Sub
End Class
