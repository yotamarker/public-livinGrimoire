grimoire memento modified
AbsDictionaryDBShadow modified

on child of AbsDictionaryDB :
abstract fun load(key: String): String //TODO set to return null as default if key not found !!!

example :
override fun load(key: String): String {
        val result = sharedPreferences.getString(key, "null")
        this.existsInDB = result != "null"
        return result!!
    }